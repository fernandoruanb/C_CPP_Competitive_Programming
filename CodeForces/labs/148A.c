#include <stdio.h>
#include <unistd.h>

int main(void) {
	int k;
	int l;
	int m;
	int n;
	int d;

	scanf("%d %d %d %d %d", &k, &l, &m, &n, &d);

	//Test multiple scanf
	//printf("%d %d %d %d %d\n", k, l, m , n, d);

	return (0);
}
